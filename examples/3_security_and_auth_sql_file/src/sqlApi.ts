// autogenerated at 2026-03-31T19:14:33.5261350+02:00


const baseUrl = "";
const parseQuery = (query: Record<any, any>) => "?" + Object.keys(query ? query : {})
    .map(key => {
        const value = (query[key] != null ? query[key] : "") as string;
        if (Array.isArray(value)) {
            return value.map((s: string) => s ? `${key}=${encodeURIComponent(s)}` : `${key}=`).join("&");
        }
        return `${key}=${encodeURIComponent(value)}`;
    })
    .join("&");

type ApiError = {status: number; title: string; detail?: string | null};
type ApiResult<T> = {status: number, response: T, error: ApiError | undefined};

interface ILoginRequest {
    username: string | null;
    password: string | null;
}

interface IWhoAmIRequest {
    _user_id?: string | null;
    _username?: string | null;
    _email?: string | null;
}

interface IWhoAmIResponse {
    userId: string | null;
    username: string | null;
    email: string | null;
}


/**
* SQL file: ../../npgsqlrest-docs/examples/3_security_and_auth_sql_file/sql/login.sql
* 
* @remarks
* HTTP POST
* @login
* @allow_anonymous
* @param $1 username
* @param $2 password
* 
* @param request - Object containing request parameters.
* @returns {ApiResult<string>}
* 
* @see SQLFILE public.login
*/
export async function login(
    request: ILoginRequest
) : Promise<ApiResult<string>> {
    const response = await fetch(baseUrl + "/api/login", {
        method: "POST",
        body: JSON.stringify(request)
    });
    return {
        status: response.status,
        response: response.ok ? await response.text() : undefined!,
        error: !response.ok && response.headers.get("content-length") !== "0" ? await response.json() as ApiError : undefined
    };
}

/**
* SQL file: ../../npgsqlrest-docs/examples/3_security_and_auth_sql_file/sql/logout.sql
* 
* @remarks
*  HTTP POST
*  @logout
*  @authorize
* 
* @returns {status: number, error: ApiError | undefined}
* 
* @see SQLFILE public.logout
*/
export async function logout() : Promise<{status: number, error: ApiError | undefined}> {
    const response = await fetch(baseUrl + "/api/logout", {
        method: "POST",
    });
    return {
        status: response.status,
        error: !response.ok && response.headers.get("content-length") !== "0" ? await response.json() as ApiError : undefined
    };
}

/**
* SQL file: ../../npgsqlrest-docs/examples/3_security_and_auth_sql_file/sql/who-am-i.sql
* 
* @remarks
* HTTP GET
* @authorize
* @user_parameters
* @param $1 _user_id default null
* @param $2 _username default null
* @param $3 _email default null
* 
* @param request - Object containing request parameters.
* @returns {ApiResult<IWhoAmIResponse[]>}
* 
* @see SQLFILE public.who-am-i
*/
export async function whoAmI(
    request: IWhoAmIRequest
) : Promise<ApiResult<IWhoAmIResponse[]>> {
    const response = await fetch(baseUrl + "/api/who-am-i" + parseQuery(request), {
        method: "GET",
        headers: {
            "Content-Type": "application/json"
        },
    });
    return {
        status: response.status,
        response: response.ok ? await response.json() as IWhoAmIResponse[] : undefined!,
        error: !response.ok && response.headers.get("content-length") !== "0" ? await response.json() as ApiError : undefined
    };
}

